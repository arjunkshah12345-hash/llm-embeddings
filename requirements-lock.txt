# Core dependency lock used for the substantive Phase 2 reference runs.
# Reference interpreter: Python 3.12.8.
# Platform-specific CUDA/MPS wheels may require a compatible torch build.
# requirements.txt remains the portable installation entry point.
matplotlib==3.11.0
numpy==2.4.6
pytest==9.1.1
tiktoken==0.13.0
torch==2.12.1
