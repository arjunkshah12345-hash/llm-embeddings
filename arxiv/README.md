# arXiv source package

This package is generated by paper/package_arxiv.py. It is a compact,
independently compilable snapshot of the manuscript, generated tables,
and referenced figures. All reported values come from the checked-in
cloud-study aggregates and compact raw artifacts.

Compile with:

    tectonic --keep-logs main.tex
